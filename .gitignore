# ---------------------------------------------------------------------------
# Sub-repositories (それぞれ独立した git リポジトリ)
# ---------------------------------------------------------------------------
# 各 sub-repo は workspace 直下に sibling として clone されているが、本 workspace
# リポジトリでは pin しない (= 個別 git 履歴で各 sub-repo を運用)。bootstrap には
# scripts/clone-all.sh を使う。
MimicX-app/
MimicX-firmware/
MimicX-protocol/
MimicX-hardware/

# ---------------------------------------------------------------------------
# Claude Code 個人設定 (per-user allowlist 等)
# ---------------------------------------------------------------------------
.claude/

# ---------------------------------------------------------------------------
# macOS / editor
# ---------------------------------------------------------------------------
.DS_Store
*.swp
.vscode/
.idea/
